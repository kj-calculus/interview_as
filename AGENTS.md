# Publishing agreement

The user authorizes automatic publication of future verified changes in this project without asking again (2026-09-11).

- After verification, commit only files belonging to the current task and push to the existing repository's `main` branch, then deploy updates to the existing online Site.
- Preserve unrelated changes and stored account data. Never force-push or overwrite divergent history.
- Stop and explain failed validation, unavailable authentication, or divergence.
- The current repository is the Sites source repository identified by `.openai/hosting.json`. No GitHub remote is configured; do not claim a Sites push is a GitHub push or create a GitHub repository without a request.
- Report the deployment URL and commit hash after successful publication.
